# Test Configuration Properties
# This file contains configurable test data for Selenium tests

# Google Test Configuration
google.url=https://www.google.com
google.expected.title=Google
google.search.query=Selenium WebDriver

# Browser Configuration
browser.default=chrome
browser.timeout.seconds=10
browser.implicit.wait.seconds=10

# Test Environment
test.environment=local
test.headless=false
